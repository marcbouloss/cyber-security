## Jad Allam

- Trained and tuned ensemble models: Random Forest, XGBoost, and LightGBM (`src/models/ensemble.py`)
- Ran full model evaluation: confusion matrices, precision-recall curves, and feature importance plots (`src/evaluate.py`)
- Built the model comparison framework and produced the final comparison chart (`reports/model_comparison_chart.png`)
- Selected LightGBM as the best model based on Macro-F1 (0.830) and ROC-AUC (0.996) on the test set
