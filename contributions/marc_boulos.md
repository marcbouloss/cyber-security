## Marc Boulos

- Built the FastAPI REST API (`src/api.py`) with single and batch prediction endpoints
- Implemented confidence thresholding and fallback logic for attack classification
- Set up Docker containerisation (Dockerfile, docker-compose.yml) and published image to Docker Hub (`marcboulos/iot-ids:latest`)
- Wrote the project README with setup and usage instructions
- Configured GitHub Actions CI/CD pipeline for automated Docker image builds
