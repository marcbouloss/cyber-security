## Karl Nguyen Van Doan

- Implemented the Logistic Regression baseline model (`src/models/baseline.py`)
- Designed and trained the PyTorch MLP neural network with custom architecture (`src/models/mlp.py`)
- Built the real-time data drift monitoring system using KS-test, PSI, and Jensen-Shannon divergence (`src/drift.py`)
- Produced the modeling analysis notebook: bias-variance tradeoff, regularisation curves, threshold tuning, and final model comparison (`notebooks/02_Modeling.ipynb`)
